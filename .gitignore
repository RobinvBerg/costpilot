# Data files (exclude from git — they can be huge)
cost-events.jsonl
cost-events-archive.jsonl
auto-logger-state.json
auto-logger-last-run.json
auto-logger-errors.log
auto-logger.pid

# Backups
backups/

# Python cache
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Annotations (personal data)
annotations.json

# BUT include the example config and the actual config template
!config.example.json
config.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Private data - never commit
Export Anthropic /
Export Anthropic*/
anthropic_ground_truth.json
*.csv
cost-events.jsonl
cost-events.jsonl.*

# Old versioned files — exclude from git
*_backup*
*_original*
dashboard_v*.html
ANALYSIS_v*.md
